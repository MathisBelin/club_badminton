<Window x:Class="BadmintonClub.Views.AssociateContactsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:BadmintonClub.Controls"
        Title="Associer des contacts à un libellé"
        Height="300" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Libellé cible" Style="{StaticResource FormLabel}" />
        <ComboBox Grid.Row="1" x:Name="LabelCombo" DisplayMemberPath="Nom"
                  Height="32" Padding="6,0" VerticalContentAlignment="Center"
                  SelectionChanged="LabelCombo_SelectionChanged" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" x:Name="EmptyLabelsHint" Foreground="#B71C1C" FontSize="11"
                   VerticalAlignment="Bottom" Visibility="Collapsed"
                   Text="Aucun libellé. Créez-en un depuis l'écran Libellés." />

        <TextBlock Grid.Row="2" Text="Adhérents à associer (uniquement les non-associés au libellé)"
                   Style="{StaticResource FormLabel}" Margin="0,14,0,4" />
        <ctrl:MultiSelectComboBox Grid.Row="3" x:Name="AdherentsSelect" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" IsCancel="True" Click="Annuler_Click" />
            <Button x:Name="AssocierButton" Content="✅ Associer"
                    Style="{StaticResource PrimaryButton}" Click="Associer_Click" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
