<Window x:Class="BadmintonClub.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Saisie"
        Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Style="{StaticResource FormLabel}" TextWrapping="Wrap" />
        <TextBox x:Name="ValueBox" Grid.Row="1" Margin="0,6,0,0" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" IsCancel="True" Click="Annuler_Click" />
            <Button Content="Valider" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="Valider_Click" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
