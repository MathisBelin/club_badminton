<Window x:Class="BadmintonClub.Views.SyncWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alertes de la synchronisation"
        Height="460" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText" Style="{StaticResource FormLabel}"
                   TextWrapping="Wrap" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" x:Name="HintText" Foreground="#777777" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,12" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Filtre :" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="#555555" FontWeight="SemiBold" />
            <ComboBox x:Name="KindCombo" Width="220" VerticalContentAlignment="Center"
                      SelectionChanged="KindFilter_Changed">
                <ComboBoxItem IsSelected="True">Tous</ComboBoxItem>
                <ComboBoxItem>Informations manquantes</ComboBoxItem>
                <ComboBoxItem>Doublons d'e-mail</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <DataGrid Grid.Row="3" x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column" SelectionMode="Extended"
                  SelectionUnit="Cell" ClipboardCopyMode="ExcludeHeader">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Type" Width="150" SortMemberPath="Kind">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TypeText}" Foreground="{Binding TypeBrush}"
                                       FontWeight="SemiBold" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*" />
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*" />
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}" Width="120" />
                <DataGridTextColumn Header="E-mail" Binding="{Binding EmailDisplay}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Left" Style="{StaticResource PrimaryButton}"
                    Content="⏳ Voir les inscriptions non finalisées" Click="OpenPending_Click" />
            <Button Grid.Column="1" Content="Fermer" IsCancel="True" Click="Fermer_Click" Margin="0" />
        </Grid>
    </Grid>
</Window>
